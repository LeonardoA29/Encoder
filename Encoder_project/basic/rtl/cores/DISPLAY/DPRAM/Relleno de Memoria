import random
import os

# Parámetros
num_celdas = 64
bits_por_celda = 16
output_file = "init_dpram.ini"
with open(output_file, "w") as f:
    for addr in range(num_celdas):
        f.write(f"\n")
        for addr in range(num_celdas):
            ##value = random.randint(0, 2**bits_por_celda - 1)
            f.write(f"0028 ")   